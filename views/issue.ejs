<%- include('includes/head.ejs') %>
</head>

<body>

    <h1>Issue</h1>

    <div class="wrapper">
        <% if (issues.length > 0) { %>
            <div class="project-container-list">
                <% for (let issue of issues) { %>
                        <div style="cursor: pointer" class="project-container">
                            Name: <%= issue.name %>
                            <br>
                            Description: <%= issue.description %>
                            <br>
                            Creation type: <%= issue.creation_type %>
                            <br>
                            <% if (issue.code_dump) { %>
                            Code dump: <%= issue.code_dump %>
                            <% } %>
                            <% if (issue.userinfo_dump) { %>
                            Information: <%= issue.userinfo_dump %>
                            <% } %>
                        </div>
                <% } %>
            </div>
        <% } else { %>
            <h1>Issue not found!</h1>
        <% } %>
    </div>

<%- include('includes/end.ejs') %>